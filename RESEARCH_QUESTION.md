# KVTide：共享前缀 KV 的提前复制是否值得？

研究备忘录，2026-09-05。状态：假设与实验设计，尚无性能或创新性结论。

## 1. Problem

共享前缀复用减少 prefill 计算，但热门前缀可能集中在繁忙实例上。
把请求送往其他实例可以分散负载，同时可能引入缓存准备成本。
KVTide 研究在请求到达之前准备额外 prefix KV 副本，能否改善这一过程。

已有系统会改变缓存分布，不能将“改变 placement”或“复制热点 KV”本身作为创新点：

- [Preble §3.2](https://arxiv.org/html/2407.00023v2) 讨论热门前缀的复制和负载重平衡；该段对物理 KV 搬运方式的描述仍需结合实现核查。
- [Mooncake §4.2](https://www.usenix.org/system/files/fast25-qin.pdf) 通过按请求触发的远端获取、本地保留和重算形成热点副本。
- [CacheRoute](https://arxiv.org/abs/2608.19677) 是近期研究前缀亲和与放置计划的预印本，也需纳入逐项对照。

候选差异是：在有限 HBM 和传输预算下，决定提前物化哪些 prefix 副本、何时开始、保留多久，以及如何承担错误预测的代价。是否形成独立贡献，须由相关工作对照和实验共同确认。

第一轮范围：同型号 GPU、相同模型及精度、每 GPU 一个模型副本、独立本地 KV cache、完整且不可变的共享前缀 blocks。暂不研究 active-request migration、P/D disaggregation、KV 压缩、分层存储、异构 GPU、容错与 Kubernetes 扩缩容。

## 2. Hypothesis

在可提前观察到需求变化、且副本准备时间影响突发请求首批服务的条件下，有选择地提前复制 prefix KV，可能比强 cache/load-aware routing、按需拉取并缓存、以及提前重算预热，更有效地降低 tail TTFT，同时满足显存、带宽和背景 decode 延迟约束。

研究问题依次为：

1. 在什么提前量、前缀长度、拥塞和需求持续时间下，提前复制具有额外收益？
2. 仅使用历史请求和当前资源状态的简单在线策略，能获得多少理想参考策略的收益？
3. 引入真实 batching、缓存竞争和传输干扰后，这些收益是否仍存在？

预期失败区域包括短前缀、低复用、短暂或错误预测的热点、带宽不足、目的地不再空闲，以及副本挤出其他高价值缓存。报告失败区域也是实验产物。

当前证据仅包括单 GPU BF16 推理、同实例前缀复用、释放、显式/自动 KV 容量 smoke。尚未验证跨 GPU KV 复制或真实 batched serving 性能。现有逐 item HF Runner 只承担正确性和初步机制探测。

## 3. Cost model

对一个到达时刻为 0 的请求，定义：

- `Qs / Qd`：源/目的实例可服务该请求之前的等待时间。
- `Hs / Hd`：已有该前缀时，从开始服务到首 token 的耗时，包含 suffix 计算。
- `Rd`：目的实例没有此前缀时，从开始服务到首 token 的完整耗时。
- `X`：从开始导出到目的实例 KV 可用的耗时，包含打包、传输、导入和同步。
- `L`：复制在请求到达前启动的提前量。

仅在传输可以独立于目的地排队推进、且忽略资源干扰时，下列近似成立：

```text
wait       ≈ Qs + Hs
recompute  ≈ Qd + Rd
pull       ≈ max(Qd, X) + Hd
early copy ≈ max(Qd, max(0, X - L)) + Hd
```

如果实现将传输与排队串行化，应按实际时序计算；不能默认有重叠。传输可能改变队列、服务时间和其他请求 ITL，最终以实测为准。

重算和 reactive pull 都会产生可保留副本。因此不能给它们每次请求重复记一次完整 `Rd` 或 `X`，却只给 proactive 记一次复制。提前复制的额外收益要相对于这些会自然变暖的基线衡量。

同时记录带宽消耗、显存占用随时间的变化、驱逐损失和未被使用的复制量。字节数不直接与毫秒相加：使用独立指标及预先确定的资源/SLO 约束。副本利用率按实际被后续请求引用的唯一复制 block 字节数除以总复制字节数计算，单独报告复用次数。

## 4. Baselines

| 策略 | 副本准备方式 |
|---|---|
| Locality-first | 在已有前缀的实例排队 |
| Least-loaded | 冷实例重算；允许之后命中并保留其自然形成的缓存 |
| Cache + load routing | 综合缓存与负载选择实例；不主动提前搬运 |
| Reactive pull + cache | 按需拉取并保留副本，合并同一前缀的并发拉取；与合理的等待/重算选择比较 |
| Oracle proactive copy | 已知未来需求，在 `-L` 开始复制 |
| Oracle proactive prefill | 与 oracle copy 使用相同未来信息、时刻和预算，在目的地提前计算相同前缀 |

两个 oracle 是带未来信息的实验参考，不是已经证明的全局最优上界。在线策略不能读取未来 trace；所有参数在独立工作负载上选择后固定。

最后一个 baseline 区分“提前准备”的价值与“复制 KV”的价值。若两者均在请求到达前完成，只能进一步比较资源开销和对背景请求的影响，不能据此声称复制降低了更多 TTFT。

各策略使用相同模型、执行引擎、batching 参数、初始缓存状态、总资源和到达序列。复制占用的空间须计入统一缓存预算，避免给予额外容量。简化实现应标为对应策略的实现，不能冒充原系统性能。

## 5. Minimal falsification experiment

### Gate A：跨实例复制正确性

先在 CPU 上用两个独立 PagedKVCache 验证导出/导入和物理 block 重映射，再在两个 GPU 进程间验证真实搬运。
导出仅包含完整有效块，按给定逻辑顺序排列，快照不与源 cache 共享存储。
目标使用独立分配的物理块，所有层的 K/V 就绪后才发布可用状态。

上层保证模型权重版本、dtype、布局、位置语义和前缀身份兼容。复制期间固定源块寿命；实验阶段可由串行协调器保证，接入并发服务时再落实 pin/refcount。
比较目的地复用导入 KV 与本地重算的 logits（指定数值容差）和输出，并验证两端独立释放。

这一步只新增实验必需的导出/导入与协调能力，不扩充完整分布式控制面。

### Gate B：机制测量与第一张交叉点图

固定一个前缀长度（首先 4k），A 有缓存、B 无缓存。
重放真实背景请求制造可控拥塞；先改变提前量与突发强度，随后扩到 1k/16k 和不同复用次数。
测量 cold prefill、local hit、真实 transfer，以及有/无背景推理时的差异。

首轮比较上表策略。计入从 `-L` 开始对所有背景请求的影响；统一排除模型加载和公共 warmup，但不免费排除某个策略独有的预计算或复制。
记录实际 P2P/主存中转路径。用 sleep 添加延迟不能替代带宽竞争实验。

交付逐请求原始记录和交叉点图；初步 HF 结果不能推广为成熟 serving 引擎的性能结论。

### Gate C：真实 batching 与在线策略

若前两关出现稳定、可解释的收益，在支持真实 batching 的成熟引擎上复核边界。
优先评估现有 KV connector，再实现只读历史统计和实时资源状态的简单策略，逐项消融提前量、准入、驻留期限与传输预算。
不以从头实现 Native Runner 或 Paged Attention kernel 为前提。

采用预先生成的 open-loop 到达序列，记录客户端 arrival、dispatch、KV ready、compute 和 first-token 时间。
主指标为客户端 TTFT 分布与固定 SLO 下的违约率；辅助指标为 ITL、有效吞吐、重算 tokens、传输量、无用副本量和缓存占用。
对多轮突发和多个随机种子重复，报告不确定性；少量请求先看时序，不能将其 P99 当稳定结论。超时和失败请求不能从总体统计中消失。

每轮保存代码 commit/未提交差异、完整依赖版本、模型 revision、驱动、GPU 拓扑、参数、trace、随机种子和原始结果。两张同机 GPU 支持有限规模机制结论，多节点或扩展性主张需要对应硬件实验。

### 继续或停止的依据

- 只胜过不保留缓存的 reactive 或每请求重复重算：修正对照，不推进 predictor。
- 与提前 prefill 无区别：尚未证实复制机制的额外价值，进一步检查资源/干扰差异。
- 在非极端负载区间可重复改善延迟且满足约束：进入在线策略和更广泛验证。
- 优势在真实 batching 下消失，或在所测合理区间连 oracle copy 都无收益：收缩/修正假设，保留负结果，不继续按原假设堆功能。

合作的首批可认领产物为相关工作差异表、跨 GPU 复制验证、以及强基线交叉点实验。研究、实现、实验与写作的责任和署名按实际贡献共同约定；先完成一个短周期产物，再决定长期投入。
